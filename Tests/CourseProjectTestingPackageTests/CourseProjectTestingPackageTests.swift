    import XCTest
    @testable import CourseProjectTestingPackage

    final class CourseProjectTestingPackageTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(CourseProjectTestingPackage().text, "Hello, World!")
        }
    }
